services:
  ws-relay:
    ports:
      - "8000:8000"

  app:
    environment:
      ORIGIN: http://localhost

  nginx:
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.dev.conf:/etc/nginx/conf.d/default.conf:ro
