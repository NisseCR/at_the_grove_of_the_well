services:
  backend:
    build: ./backend
    env_file: .env
    environment:
      ASSETS_DIR: /assets
    volumes:
      - ${ASSETS_DIR}:/assets:ro
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
