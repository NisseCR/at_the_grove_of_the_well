services:
  backend:
    build: ./backend
    env_file: .env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        PUBLIC_ASSETS_BASE: ${PUBLIC_ASSETS_BASE}
    depends_on:
      - backend
    restart: unless-stopped
